package service;import junit.framework.TestCase;import org.slf4j.Logger;import org.slf4j.LoggerFactory;/** * Created by amirnashat on 8/23/16. */public class SubnetServiceTest extends TestCase {	final Logger logger = LoggerFactory.getLogger(SubnetServiceTest.class);		public void testProcessSubnet() {//		AWSService subnetService = new SubnetService();////		subnetService.process();	}	public void testVerifyIncorrectSubnet() {//		SubnetService subnetService = new SubnetService();//		List<Subnet> subnets = new ArrayList<Subnet>();////		Subnet subnet = new Subnet();//		subnet.setAvailabilityZone("us-east-1c");//		subnet.setSubnetName("private-1");////		subnets.add(subnet);////		Map incorrectSubnets = subnetService.verifySubnets(subnets);////		assertEquals(1,incorrectSubnets.size());	}	public void testVerifyCorrectSubnet() {//		SubnetService subnetService = new SubnetService();//		List<Subnet> subnets = new ArrayList<Subnet>();////		Subnet subnet = new Subnet();//		subnet.setAvailabilityZone("us-east-1c");//		subnet.setSubnetName("private-1c");////		subnets.add(subnet);////		Map correctSubnets = subnetService.verifySubnets(subnets);////		assertEquals(0,correctSubnets.size());	}	}