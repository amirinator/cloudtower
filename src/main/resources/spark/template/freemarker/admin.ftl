<html><#include "./navigation.ftl"><body><h2>Admin</h2><table>    <form method="post" action="/admin/usage/update" >        <!-- Field for usage name -->        <div class="form-group">            <label class"formLabel"> Account </label>            <select class="form-label" name="account">            <#list accounts as id , name >                <option value='${id}'>${name}</option>            </#list>            </select>            <label class"form-label"> Service Type </label>            <select class="form-label" name="usageType">            <#list usagesTypes as usage>                <option value='${(usage!" ")}' <#if usage == defaultUsage> selected = "selected"</#if> >${usage}</option>            </#list>            </select>            <label class"formLabel"> Service Limit </label>            <input name="maxAllowed" type="text" maxlength="10" />            <input type="Submit" value="Submit">        </div>    </form></table></body></html>